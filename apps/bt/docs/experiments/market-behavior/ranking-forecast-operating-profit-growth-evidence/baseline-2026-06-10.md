# Baseline 2026-06-10

Run id: `20260610_ranking_forecast_op_growth_prime_v8`

Command:

```bash
uv run --project apps/bt python apps/bt/scripts/research/run_ranking_forecast_operating_profit_growth_evidence.py --horizons 5,10,20,60 --markets prime --run-id 20260610_ranking_forecast_op_growth_prime_v8 --notes "Daily Ranking Research Base canonical overvalued internal columns"
```

Bundle:

`~/.local/share/trading25/research/market-behavior/ranking-forecast-operating-profit-growth-evidence/20260610_ranking_forecast_op_growth_prime_v8`

Key reproducibility notes:

- Source DB: `~/.local/share/trading25/market-timeseries/market.duckdb`
- Source mode: `live`
- Observation count: `4,779,812`
- Code count: `2,438`
- Date count: `2,458`
- Growth ratio coverage: `72.28%`
- Required runtime tables: `stock_data`, `topix_data`, `daily_valuation`, `stock_master_daily`, `indices_data`, `index_master`
- `statements` is not required for the fast path. The research uses `p_op / forward_p_op` from `daily_valuation` as the PIT daily valuation-basis equivalent of `forecast operating profit / operating profit`.
- This baseline explicitly uses Daily Ranking Research Base valuation signal columns (`Deep Value`, `Overvalued`, `Very Overvalued`, `No Earnings`) and crosses them with `Long Hybrid Leadership`, `ATR20 Accel`, current `Sector Score`, `crowded_rerating`, `stale_liquidity`, low/missing growth, contraction, and ATR overheat / no-value short states.

Primary result tables:

- `growth_bucket_evidence_df`
- `valuation_growth_ratio_evidence_df`
- `decision_scope_growth_evidence_df`
- `long_deep_dive_growth_evidence_df`
- `short_deep_dive_growth_evidence_df`

Implementation note:

This run depends on reusable Daily Ranking Research Base columns. The Base SoT emits `overvalued_warning` / `very_overvalued_warning` directly and does not emit positive-sounding compatibility aliases:

- `forecast_operating_profit_growth_ratio`
- `forecast_operating_profit_growth_pct`
- `per_to_fop_growth_ratio`
- `per_to_fop_growth_ratio_percentile`
- `forward_per_to_fop_growth_ratio`
- `forward_per_to_fop_growth_ratio_percentile`
- `valuation_signal`
- `strong_value_confirmation`
- `medium_value_confirmation`
- `overvalued_warning`
- `very_overvalued_warning`
- `no_positive_earnings_valuation`
- `no_value_confirmation`

The runner calls `create_daily_ranking_research_panel(..., include_liquidity_ranked=True)` so short/red feature definitions and sector overlays can reuse liquidity-scope reranking without rebuilding a separate base panel.

Superseded run:

- `20260610_ranking_forecast_op_growth_prime_v7` aligned reader-facing result labels but was generated before the Base SoT removed positive-sounding compatibility aliases.
- `20260610_ranking_forecast_op_growth_prime_v6` aligned short deep-dive labels to Overvalued wording but still emitted one broad decision-scope label with legacy wording.
- `20260610_ranking_forecast_op_growth_prime_v5` connected the runner to Base valuation signals but still emitted some result labels with legacy wording.
- `20260610_ranking_forecast_op_growth_prime_v4` explicitly crossed sector/ATR/Long Hybrid but still framed valuation through local low/high-PER-style scopes instead of the Base valuation signal SoT.
- `20260610_ranking_forecast_op_growth_prime_v3` was the initial shallow pass with only `growth_bucket_evidence_df`, `valuation_growth_ratio_evidence_df`, and `decision_scope_growth_evidence_df`.

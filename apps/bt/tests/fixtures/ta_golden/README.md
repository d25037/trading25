# TA Golden Fixtures

This directory is the source of truth for bt-side indicator golden regression tests.

Files were moved from `apps/ts/packages/domain/src/ta/__fixtures__/golden` when the
legacy ts domain package was removed, so bt regression tests no longer depend on a
ts-internal package layout.

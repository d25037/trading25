# CI Issues 472-476 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore the failing `main` CI run and complete the Market v4 operator path described by GitHub issues #472 through #476.

**Architecture:** Keep production Market v4 activation fail-closed and isolate platform-specific filesystem guarantees at the test/runner boundary. Fix the independent TypeScript presentation, research target classification, dependency lock, and operator documentation problems without broad refactoring. Provision the cutover smoke strategy as a repository fallback under the existing production strategy resolution contract.

**Tech Stack:** Python 3.12, pytest, uv, FastAPI service tests, Bun, React 19, Vitest, Ruff, GitHub Actions, YAML strategy configuration.

## Global Constraints

- Preserve Darwin atomic directory exchange semantics; do not add rename/copy fallback.
- Deleted research paths remain available to classification guardrails but never become filesystem execution targets.
- Use one canonical yen glyph consistently across Daily Ranking.
- Resolve Pillow to `12.3.0` or later without vulnerability ignores.
- Production strategy YAML must explicitly declare `shared_config.data_source: market` and `shared_config.universe_preset`.
- The full rebuild operator flow is `stop writers → preflight → isolated full rehearsal → immutable verified backup → active full-rebuild cutover → validate → rebuild required dataset snapshots`.
- Preserve the user's untracked `.codex/config.toml`.

---

### Task 1: Platform-correct Market v4 cutover tests (#472)

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `apps/bt/tests/unit/server/services/test_market_v4_cutover_*.py`
- Modify only if required by the confirmed root cause: `apps/bt/src/infrastructure/db/market/managed_root.py`
- Modify only if required by the confirmed root cause: `apps/bt/src/application/services/market_v4_cutover/**/*.py`

**Interfaces:**
- Consumes: the production atomic exchange and inherited-descriptor contracts.
- Produces: explicit test capability/runner behavior that leaves unsupported production operations fail-closed.

- [ ] Reproduce representative Linux failures from CI run `29530632461`.
- [ ] Add a regression assertion that distinguishes portable logic from Darwin-only atomic exchange behavior.
- [ ] Run the focused test and confirm the pre-fix failure.
- [ ] Implement the smallest test/runner capability change without adding production fallback.
- [ ] Run `uv run --directory apps/bt pytest tests/unit/server/services/test_market_v4_cutover_*.py tests/unit/cli_bt/test_market_cutover_cli.py`.

### Task 2: Canonical Daily Ranking yen display (#473)

**Files:**
- Modify: `apps/ts/packages/web/src/components/Ranking/DailyRankingSnapshot.test.tsx`
- Modify: `apps/ts/packages/web/src/components/Ranking/dailyRankingPresentation.test.tsx`
- Modify only if the implementation is the outlier: the shared Daily Ranking currency formatter.

**Interfaces:**
- Consumes: the existing Daily Ranking formatter output.
- Produces: one canonical Japanese yen glyph in formatter and component expectations.

- [ ] Search Ranking sources and tests for both `¥` and `￥`.
- [ ] Run the two focused tests and confirm the glyph mismatch.
- [ ] Update the stale side of the contract.
- [ ] Run the focused Ranking tests.
- [ ] Run `bun run workspace:test` from `apps/ts`.

### Task 3: Exclude deleted research paths from executable targets (#474)

**Files:**
- Modify: `scripts/ci/research-test-targets.py`
- Modify: the existing tests for `research-test-targets.py` under `scripts/ci` or `apps/bt/tests/unit/scripts`.

**Interfaces:**
- Consumes: git name/status change records.
- Produces: classification output retaining deletions and `py-files` output containing only current regular Python files.

- [ ] Add a deleted-research-file regression case.
- [ ] Run the focused test and confirm the deleted path appears in `py-files`.
- [ ] Filter executable output by status/existence while preserving fail-closed classification.
- [ ] Run the research target and taxonomy tests.
- [ ] Reproduce the current large deletion diff target generation and confirm Ruff receives no missing path.

### Task 4: Resolve Pillow vulnerabilities (#475)

**Files:**
- Modify: `apps/bt/uv.lock`

**Interfaces:**
- Consumes: transitive Pillow constraints from `vectorbt → imageio/matplotlib`.
- Produces: a lock resolving Pillow `12.3.0` or later.

- [ ] Confirm Pillow is transitive with `uv tree --project apps/bt --invert --package pillow`.
- [ ] Run `uv lock --project apps/bt --upgrade-package pillow`.
- [ ] Confirm the lock contains a fixed Pillow version.
- [ ] Run the report/image-related focused tests.
- [ ] Run the same `pip-audit` command used by CI.

### Task 5: Complete full rebuild runbook and smoke strategy (#476)

**Files:**
- Create: `apps/bt/config/strategies/production/cutover_smoke.yaml`
- Modify: `docs/runbooks/market-v4-cutover.md`
- Modify: `apps/bt/tests/unit/server/services/test_market_v4_cutover_cutover_contracts.py`

**Interfaces:**
- Consumes: `bt market-cutover preflight|rehearse|backup|cutover|restore`, strategy strict validation, and screening profile compilation.
- Produces: a clean-checkout-capable `production/cutover_smoke` and exact operator commands for both full rebuild and retained promotion.

- [ ] Add a test requiring the repository smoke YAML, strict validation, supported screening profile, and explicit entry-decidability classification.
- [ ] Add runbook contract assertions for writer shutdown, shell-loaded `JQUANTS_PLAN`, `preflight → rehearse → backup → cutover`, validation/restore, Web RESET risk, retained-evidence eligibility, and dataset schemaVersion 3 recreation.
- [ ] Run the focused tests and confirm they fail before files are added.
- [ ] Add the minimal repository fallback strategy with explicit Market universe scope.
- [ ] Rewrite the runbook with exact full rebuild and retained promotion flows.
- [ ] Run strategy config, screening profile, cutover contract, and CLI tests.

### Task 6: Integrated verification and publication

**Files:**
- Review all files changed by Tasks 1-5.

**Interfaces:**
- Consumes: all independent fixes.
- Produces: one reviewed branch with evidence for closing #472-#476.

- [ ] Run Python lint/type checks for changed Python files.
- [ ] Run the complete backend package unit-test command used by CI.
- [ ] Run `bun run workspace:test`.
- [ ] Run the research CI target flow and dependency audit.
- [ ] Review `git diff --check` and ensure `.codex/config.toml` is untracked and untouched.
- [ ] Commit, push `codex/fix-issues-472-476`, and open a PR referencing `Closes #472`, `Closes #473`, `Closes #474`, `Closes #475`, and `Closes #476`.
